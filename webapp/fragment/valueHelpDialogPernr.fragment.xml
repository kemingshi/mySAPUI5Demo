<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <SelectDialog
        title="{i18n>Pernr}"
        class="sapUiPopupWithPadding"
        items="{/PersonSet}"
        search="_handleValueHelpSearch"
        confirm="_handleValueHelpClose"
        cancel="_handleValueHelpClose">
        <StandardListItem
            title="{Pernr}"
            description="{Nachn}{Vorna}" />
    </SelectDialog>
</core:FragmentDefinition>